# GDJ - 12-30-2021 - This file is to be used only as a reference. XCONF is no longer owned by FAR.
#
# Hatsmaker template file for HAProxy for use by XCONF.
# IPv6 support is assumed.

global
        daemon
        pidfile /var/run/haproxy.pid
        maxconn 120000
        nbproc 2
        group haproxy
        user haproxy

	ssl-default-bind-ciphers ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA256:ECDHE-ECDSA-AES128-SHA:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES128-SHA:ECDHE-ECDSA-AES256-SHA384:ECDHE-ECDSA-AES256-SHA:ECDHE-RSA-AES256-SHA:DHE-RSA-AES128-SHA256:DHE-RSA-AES128-SHA:DHE-RSA-AES256-SHA256:DHE-RSA-AES256-SHA:ECDHE-ECDSA-DES-CBC3-SHA:ECDHE-RSA-DES-CBC3-SHA:EDH-RSA-DES-CBC3-SHA:AES128-GCM-SHA256:AES256-GCM-SHA384:AES128-SHA256:AES256-SHA256:AES128-SHA:AES256-SHA:DES-CBC3-SHA:!DSS

	ssl-default-bind-options no-sslv3 no-tls-tickets

	ssl-default-server-ciphers ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA256:ECDHE-ECDSA-AES128-SHA:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES128-SHA:ECDHE-ECDSA-AES256-SHA384:ECDHE-ECDSA-AES256-SHA:ECDHE-RSA-AES256-SHA:DHE-RSA-AES128-SHA256:DHE-RSA-AES128-SHA:DHE-RSA-AES256-SHA256:DHE-RSA-AES256-SHA:ECDHE-ECDSA-DES-CBC3-SHA:ECDHE-RSA-DES-CBC3-SHA:EDH-RSA-DES-CBC3-SHA:AES128-GCM-SHA256:AES256-GCM-SHA384:AES128-SHA256:AES256-SHA256:AES128-SHA:AES256-SHA:DES-CBC3-SHA:!DSS

        tune.ssl.default-dh-param 1024

        log 127.0.0.1:1514 local1 info
        log-send-hostname
	stats socket /var/run/haproxy.sock mode 666 level user

defaults
        mode http
        option httplog
        option log-health-checks
        option log-separate-errors
        option http-keep-alive
        option clitcpka
        timeout connect 5s
        timeout client 15s
        timeout server 15s
        balance roundrobin
        default-server inter 5s fall 3 rise 1
        monitor-uri /haproxy_test
        monitor-net 192.168.3.2/31
        monitor-net 96.114.160.2/31

listen stats
  bind 0.0.0.0:10543
  mode http
  log global
  stats uri /haproxy-stats

listen statsipv6
  bind ipv6@:10543
  mode http
  log global
  stats uri /haproxy-stats

# one frontend per service
<#list tsp.configs as service>

#
# Ipv4 front end
#
frontend ${service.service}
    bind ipv4@:${service.port} name ${service.service}
    log global
    maxconn ${service.maxconn}
    option httplog
    option forwardfor if-none
    http-request set-header HA-Forwarded-For %ci
    capture request header X-Forwarded-For len 64
    capture request header X-ServiceAccountId len 64
    capture request header X-ServiceDeviceId len 64
    capture request header User-Agent len 64
    http-request add-header HA-Haproxy-xconf-http "xconf-http"

    # acl swu_stb_in_req path_reg /swu/stb

    # option http-buffer-request
    # http-request deny unless METH_POST swu_stb_in_req { req.body -m reg "B-DRI" }

    acl is_head method HEAD
    use_backend return405 if is_head

    <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "in">
    acl ${service.service}-${rule_index} hdr(${rule.header}) -f ${rule.file}
        <#break>
        <#case "bitmask">
    acl ${service.service}-${rule_index} hdr(${rule.header}),bitmask(${rule.value}) eq 1
        <#break>
        <#case "percent">
    acl ${service.service}-${rule_index} hdr(${rule.header}),percent(${rule.value}) eq 1
        <#break>
        <#case "like">
    acl ${service.service}-${rule_index} hdr_reg(${rule.header}) ${rule.value}
        <#break>
      </#switch>
    </#if>
    </#list>

# Backend rules are evaluated in the order that they appear.  The first match is the one that is used.  The ssl backends
# come before the non-ssl backends.  The default ssl backend comes before the default non-ssl backend.
  <#assign default="">
  <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "default">
        <#assign default=rule.version>
        <#break>
        <#case "in">
        <#case "bitmask">
        <#case "percent">
        <#case "like">
    use_backend ${service.service}.${rule.version} if ${service.service}-${rule_index}
      </#switch>
    </#if>
  </#list>
  <#if default != "">
    default_backend ${service.service}.${default}
  </#if>

#
# Ipv4 SSL front end
#
frontend ${service.service}_ssl
    bind ipv4@:${service.sslPort} ssl crt ${service.sslCert}
    log global
    maxconn 10000
    option httplog
    option forwardfor if-none
    http-request set-header HA-Forwarded-For %ci
    capture request header X-Forwarded-For len 64
    capture request header X-ServiceAccountId len 64
    capture request header X-ServiceDeviceId len 64
    capture request header User-Agent len 64

    acl is_head method HEAD
    use_backend return405 if is_head

    <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "in">
    acl ${service.service}-${rule_index} hdr(${rule.header}) -f ${rule.file}
        <#break>
        <#case "bitmask">
    acl ${service.service}-${rule_index} hdr(${rule.header}),bitmask(${rule.value}) eq 1
        <#break>
        <#case "percent">
    acl ${service.service}-${rule_index} hdr(${rule.header}),percent(${rule.value}) eq 1
        <#break>
        <#case "like">
    acl ${service.service}-${rule_index} hdr_reg(${rule.header}) ${rule.value}
        <#break>
      </#switch>
    </#if>
    </#list>

# Backend rules are evaluated in the order that they appear.  The first match is the one that is used.  The ssl backends
# come before the non-ssl backends.  The default ssl backend comes before the default non-ssl backend.
  <#assign default="">
  <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "default">
        <#assign default=rule.version>
        <#break>
        <#case "in">
        <#case "bitmask">
        <#case "percent">
        <#case "like">
    use_backend ${service.service}.${rule.version} if ${service.service}-${rule_index}
      </#switch>
    </#if>
  </#list>
  <#if default != "">
    default_backend ${service.service}.${default}
  </#if>

#
# Ipv6 front end
#
frontend ${service.service}_v6
    bind ipv6@:${service.port} name ${service.service}
    log global
    maxconn 10000
    option httplog
    option forwardfor if-none
    http-request set-header HA-Forwarded-For %ci
    capture request header X-Forwarded-For len 64
    capture request header X-ServiceAccountId len 64
    capture request header X-ServiceDeviceId len 64
    capture request header User-Agent len 64
    http-request add-header HA-Haproxy-xconf-http "xconf-http"

    # acl swu_stb_in_req path_reg /swu/stb

    # option http-buffer-request
    # http-request deny unless METH_POST swu_stb_in_req { req.body -m reg "B-DRI" }

    acl is_head method HEAD
    use_backend return405 if is_head

    <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "in">
    acl ${service.service}-${rule_index} hdr(${rule.header}) -f ${rule.file}
        <#break>
        <#case "bitmask">
    acl ${service.service}-${rule_index} hdr(${rule.header}),bitmask(${rule.value}) eq 1
        <#break>
        <#case "percent">
    acl ${service.service}-${rule_index} hdr(${rule.header}),percent(${rule.value}) eq 1
        <#break>
        <#case "like">
    acl ${service.service}-${rule_index} hdr_reg(${rule.header}) ${rule.value}
        <#break>
      </#switch>
    </#if>
    </#list>

# Backend rules are evaluated in the order that they appear.  The first match is the one that is used.  The ssl backends
# come before the non-ssl backends.  The default ssl backend comes before the default non-ssl backend.
  <#assign default="">
  <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "default">
        <#assign default=rule.version>
        <#break>
        <#case "in">
        <#case "bitmask">
        <#case "percent">
        <#case "like">
    use_backend ${service.service}.${rule.version} if ${service.service}-${rule_index}
      </#switch>
    </#if>
  </#list>
  <#if default != "">
    default_backend ${service.service}.${default}
  </#if>

#
# Ipv6 front end
#
frontend ${service.service}_v6_ssl
    bind ipv6@:${service.sslPort} ssl crt ${service.sslCert}
    log global
    maxconn 10000
    option httplog
    option forwardfor if-none
    http-request set-header HA-Forwarded-For %ci
    capture request header X-Forwarded-For len 64
    capture request header X-ServiceAccountId len 64
    capture request header X-ServiceDeviceId len 64
    capture request header User-Agent len 64

    acl is_head method HEAD
    use_backend return405 if is_head

    <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "in">
    acl ${service.service}-${rule_index} hdr(${rule.header}) -f ${rule.file}
        <#break>
        <#case "bitmask">
    acl ${service.service}-${rule_index} hdr(${rule.header}),bitmask(${rule.value}) eq 1
        <#break>
        <#case "percent">
    acl ${service.service}-${rule_index} hdr(${rule.header}),percent(${rule.value}) eq 1
        <#break>
        <#case "like">
    acl ${service.service}-${rule_index} hdr_reg(${rule.header}) ${rule.value}
        <#break>
      </#switch>
    </#if>
    </#list>

# Backend rules are evaluated in the order that they appear.  The first match is the one that is used.  The ssl backends
# come before the non-ssl backends.  The default ssl backend comes before the default non-ssl backend.
  <#assign default="">
  <#list tsp.rules as rule>
    <#if rule.service == service.service>
      <#switch rule.operation>
        <#case "default">
        <#assign default=rule.version>
        <#break>
        <#case "in">
        <#case "bitmask">
        <#case "percent">
        <#case "like">
    use_backend ${service.service}.${rule.version} if ${service.service}-${rule_index}
      </#switch>
    </#if>
  </#list>
  <#if default != "">
    default_backend ${service.service}.${default}
  </#if>
</#list>

# one backend per service and version
backend return405
    errorfile 503 /etc/haproxy/errors/405.http

<#list tsp.configs as service>
  <#if service.backendMaxconn?has_content >
    <#assign be_maxconn="${service.backendMaxconn}">
  <#elseif service.service == "appdiscoveryService">
    <#assign be_maxconn="2">
  <#else>
    <#assign be_maxconn="200">
  </#if>
  <#if service.sslBackend?has_content && service.sslBackend == "true" >
    <#assign be_ssl=true >
  <#else>
    <#assign be_ssl=false >
  </#if >
  <#list service.versions as v>

backend ${service.service}.${v.version}
    <#if service.healthCheck?has_content >
    option httpchk GET ${service.healthCheck}
    <#elseif service.heartBeat?has_content >
    option httpchk GET ${service.heartBeat}
    </#if>
    <#list v.hosts as host>
    <#if service.sslPort?has_content && (be_ssl || service.sslPort == host.port ) >
    server ${host.host} ${host.ip}:${service.sslPort} ssl verify none maxconn ${be_maxconn} check
    <#else>
    server ${host.host} ${host.ip}:${host.port} maxconn ${be_maxconn} check
    </#if>
    </#list>
  </#list>
</#list>

