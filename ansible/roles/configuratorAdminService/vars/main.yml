---
# vars file for configuratorAdminService
ELK_USERNAME: "{{ lookup('hashi_vault', 'secret=/secret/appds/far/elk/xreconfigurator/cluster_user:value auth_method=approle role_id=configuratorAdminService secret_id=None url=https://current.vault.do.xcal.tv:8443') }}"
ELK_PASSWORD: "{{ lookup('hashi_vault', 'secret=/secret/appds/far/elk/xreconfigurator/cluster_pass:value auth_method=approle role_id=configuratorAdminService secret_id=None url=https://current.vault.do.xcal.tv:8443') }}"
ghe_access_token: "{{ lookup('hashi_vault', 'secret=/secret/appds/far/elk/xreconfigurator/ghec_access_token:value auth_method=approle role_id=configuratorAdminService secret_id=None url=https://current.vault.do.xcal.tv:8443') }}"